include <stdio.h>
int main() {
    printf("kavinesh");#
    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);

    if(num % 2 == 0)
        printf("%d is even.", num);
    else
        printf("%d is odd.", num);
    
    return 0;
}

output
kavineshEnter an integer: -7
-7 is odd.
